'''class Car:
    def __init(self, engine, wheels, lights):
        #self.engine = Engine(litres, fuel)
        #self.wheels = Wheels(tire, radius)
        #self.lights = Lights(watts, location)

class Engine:
    def __init__(self, litres, fuel):
        self.litres = litres
        self.fuel = fuel

class Wheels:
    def __init__(self, tire, radius):
       self.radius = radius
       self.tire = Tires(self.radius)

class Tires:
    def __init__(self, radius):
        self.radius = radius

class Lights:
    def __init__(self, watts):
        self.watts = watts
        self.location = location

engine = Engine(100, "Diesel")
tire = Tires(100)
front_lights = Lights(85)
wheels = Wheels(tire)'''